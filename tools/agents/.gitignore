# Regenerated by the setup scripts; never committed.
node_modules/
runs/
hub/data/
hub/node_modules/
git/stride.git/
git/clones/
# A token belongs in the hub, not on disk - runner.ps1 fetches it per run.
codex-token.txt
